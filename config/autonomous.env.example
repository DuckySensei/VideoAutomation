# Copy to config/autonomous.env (ignored by git) or set $env: in PowerShell.
# Do not put real secrets in this example file.

# Google OAuth (use OAuth Playground or your flow to get refresh token)
YT_CLIENT_ID=your_client_id.apps.googleusercontent.com
YT_CLIENT_SECRET=your_client_secret
YT_REFRESH_TOKEN=your_refresh_token
YT_TOKEN_URI=https://oauth2.googleapis.com/token

# Optional: enable AI-written scripts (if unset, local templates are used)
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4.1-mini

# Hook commands (paths relative to repo root)
YOUTUBE_UPLOAD_COMMAND=python src/integrations/upload_to_youtube.py --video "{video_path}" --title "{title}" --description "{description}" --privacy "{privacy_status}" --publish-at "{publish_at}"
TIKTOK_UPLOAD_COMMAND=python src/integrations/upload_to_tiktok.py --video "{video_path}" --caption "{caption}" --publish-at "{publish_at}"
INSTAGRAM_UPLOAD_COMMAND=python src/integrations/upload_to_instagram.py --video "{video_path}" --caption "{caption}" --publish-at "{publish_at}"
METRICS_IMPORT_COMMAND=python src/integrations/pull_youtube_metrics.py --base-dir . --days 1 --variable-cost-usd 0.02

# Instagram Graph API settings (required by upload_to_instagram.py)
# INSTAGRAM_ACCESS_TOKEN=your_long_lived_user_or_page_token
# INSTAGRAM_ACCOUNT_ID=1784...
# INSTAGRAM_VIDEO_URL_BASE=https://your-public-cdn-or-bucket/videos
